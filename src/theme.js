const typography = {
    fontFamily: [
        'Comfortaa', 'cursive'
    ].join(','),
};

export const DefaultTheme = {
    typography: typography
};

export const DarkTheme ={
    typography: typography,
    palette: {
        type: "dark"
    }
};
